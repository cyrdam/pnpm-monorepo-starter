{
  "name": "pnpm-monorepo-starter",
  "version": "1.0.0",
  "description": "pnpm starter template for monorepo projects",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm run --recursive serve",
    "lint": "pnpm run -r lint",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "monorepo",
    "pnpm"
  ],
  "author": "Damian Cyrus <damian.cyrus@diconium.com>",
  "license": "ISC",
  "engines": {
    "pnpm": ">=6",
    "node": ">=14"
  }
}
